---
title: Find the Largest Number in an Array
description: Write a function that finds the largest number in an array.
date: "2024-03-20"
language: "javascript"
---

## Question
Write a function that receives an array of numbers and returns the largest number in it.


<CodeEditor 
    initialCode={"cons findLargest = (myArray) => {\n...\n}"}
    checkTests={[
        {
        call: "findLargest([100,10,20])",
        expectedResult: 100
        },
        {
        call: "findLargest([-5, -2, 0])",
        expectedResult: 0
        },
      {
         call: "findLargest([-1])",
         expectedResult: -1,
      },
        {
        call: "findLargest([-3,30,9,-9,0,0,0])",
        expectedResult: 30,
        },
        {
         call: "findLargest([])",
         expectedResult: undefined,
      },
      {
         call: "findLargest([0])",
         expectedResult: 0,
         hidden: true
      }
        ]}
/>

<details>
  <summary>See Solution</summary>

## Math library
```ts
const findLargest = (myArray) => {
   if(myArray.length === 0) return undefined
   return Math.max(...myArray)
}
```

### Explanation
The Math.max() function is used to find the largest number in a list of numbers. 
The spread operator is used to pass the elements of the array as arguments to the Math.max() function.

## Complexity Analysis
The time complexity for this solution is O(n) where n is the number of elements in the array.

## For loop
### For loop
```ts
const findLargest = (myArray) => {
   if(myArray.length === 0) return undefined
   let largest = myArray[0]
   for(let i = 1; i < myArray.length; i++){
      if(myArray[i] > largest){
         largest = myArray[i]
      }
   }
   return largest
}
```

## Lodash
```ts
import {max} from lodash

const findLargest = (myArray) => {
   return max(myArray)
}
```

### Array sorting
```ts
const findLargest = (myArray) => {
   return myArray.sort((a,b) => b - a)[0]
}
```

</details>