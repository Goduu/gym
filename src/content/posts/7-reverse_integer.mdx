---
id: 7
title: Reverse an Integer
description: Write a function that reverses an integer.
date: "2024-03-20"
language: "javascript"
---

## Question
Write a function that reverses an integer.

<CodeEditor 
    initialCode={"cons reverseInteger = (myInteger) => {\n...\n}"}
    checkTests={[
        {
         id: 1,
        call: "reverseInteger(987)",
        expectedResult: 789
        },
        {
         id: 2,
        call: "reverseInteger(1234)",
        expectedResult: 4321
        },
        {
         id: 3,
        call: "reverseInteger(-12)",
        expectedResult: -21
        },
        {
         id: 4,
        call: "reverseInteger(-123)",
        expectedResult: -321,
        hidden: true
        },
         {
            id: 5",
            call: "reverseInteger(0)",
            expectedResult: 0,
            hidden: true
         },
        ]}
/>

<details>
  <summary>See Solution</summary>

```ts
const reverseInteger = (myInteger) => {
   const reversed =  parseInt(myInteger.toString().split('').reverse().join(''))
   return Math.sign(myInteger) * reversed
}
```

### Explanation
The function takes an integer as an argument and converts it to a string.
It then splits the string into an array of characters, reverses the array, 
and joins it back to a string. The string is then converted back to an integer. 
The function then multiplies the integer by the sign of the original integer to ensure that the result has the same sign as the original integer.

## Complexity Analysis
The time complexity for this function is O(n) where n is the number of digits in the integer.

## Alternatives

### For loop
```js
const reverseInteger = (myInteger) => {
      let reversed = 0
      while(myInteger !== 0){
         reversed = reversed * 10 + myInteger % 10
         myInteger = parseInt(myInteger / 10)
      }
      return reversed
   }
```

### Lodash
```ts
import { reverse } from 'lodash'

const reverseInteger = (myInteger) => {
   return parseInt(reverse(myInteger.toString().split('')).join(''))
}

```

</details>