---
id: 8
title: Remove Duplicates from an Array
description: Write a function that removes duplicate elements from an array.
date: "2024-03-20"
language: "javascript"
---

## Question
Write a function that removes duplicate elements from an array.

<CodeEditor 
    initialCode={"cons removeDuplicates = (myArray) => {\n...\n}"}
    checkTests={[
        {
        call: "removeDuplicates([1,2,3])",
        expectedResult: [1,2,3]
        },
        {
        call: "removeDuplicates([1,1,1,1])",
        expectedResult: [1]
        },
        {
           call: "removeDuplicates([])",
           expectedResult: []
        },
        {
        call: "removeDuplicates([1,1,1,2,2,1,2,9,7])",
        expectedResult: [1,2,9,7],
        hidden: true
        },
        ]}
/>

<details>
  <summary>See Solution</summary>

```js
const removeDuplicates = (myArray) => {
   return Array.from(new Set(myArray))
}
```

### Explanation
- We use the `Set` object to store unique values of any type.
- We then convert the `Set` object to an array using the `Array.from` method.


## Complexity Analysis
The time complexity for this solution is O(n) where n is the length of the input array.

## Alternative
### For loop
```ts
function removeDuplicates(myArray) {
      let uniqueArray = [];
      for (let i = 0; i < myArray.length; i++) {
         if (uniqueArray.indexOf(myArray[i]) === -1) {
               uniqueArray.push(myArray[i]);
         }
      }
      return uniqueArray;
}
```

### Spreading
```ts
function removeDuplicates(myArray) {
    return [...new Set(myArray)];
}
```

</details>